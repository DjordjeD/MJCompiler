program test

{
	void input(int array[], int n) int i; {
		i = 0;
		do {
			read(array[i]);
			i++;
		} while (i < n);
	}

	void sort(int array[], int n) int i; int j; int minInd; int tmp; {
		i = 0;
		do {
			minInd = i;
			j = i+1;
			if (j == n) return;
			do {
				if (array[j] < array[minInd]) {
					minInd = j;
				}
				j++;
			} while (j < n);

			tmp = array[i];
			array[i] = array[minInd];
			array[minInd] = tmp;

			i++;
		} while (i < n);
	}

	void output(int array[], int n) int i; {
		i = 0;
		do {
			print(array[i]);
			i++;
		} while (i < n);
	}
	void main() int arr[]; int n; {
		read(n);
		if (n <= 0) {
			return;
		}

		arr = new int[n];
		input(arr, n);
		sort(arr, n);
		output(arr, n);
	}
}
